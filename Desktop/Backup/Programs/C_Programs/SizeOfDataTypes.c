#include<stdio.h>
int main()
{
    int a;
    char b;
    float c;
    double d;
    printf("The size of integer datatype is %d bytes.\n",sizeof(a));
    printf("The size of charectar datatype is %d bytes.\n",sizeof(b));
    printf("The size of floating datatype is %d bytes.\n",sizeof(c));
    printf("The size of double datatype is %d bytes.\n",sizeof(d));
}